# NEXUS 领域词汇

## 开发任务

用户发起的一次可恢复工作单元，包含输入、工作区、模型、状态和结果摘要。开发任务可以选择普通执行或协作分析。

## 协作分析

围绕一个开发任务，由主 Agent 受控分派多个具有固定职责的子 Agent，收集结构化产物并生成统一的执行建议。协作分析本身不直接执行写入、命令、Git、部署或删除操作。

## 子 Agent

协作分析中的受限分析角色。每个子 Agent 只接收完成职责所需的最小上下文，只能使用其被授予的只读工具，不允许继续派生子 Agent。

## 协作轮次

一组子 Agent 针对同一任务上下文完成一次独立分析并提交产物。首版最多两轮；下一轮只能消费上一轮的结构化摘要。

## 协作产物

子 Agent 提交的结构化结论，包含摘要、发现、证据、风险和建议。产物可以被其他角色引用，但不等同于已验证事实。

## 执行审批

主 Agent 汇总协作产物后形成的单一执行计划需要用户再次确认。协作分析阶段不因模型建议而自动产生文件、命令、Git 或网络副作用。
