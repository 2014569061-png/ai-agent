# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/build/
/android/.gradle/
/android/local.properties

# Widget Preview related
.widget_preview/
android/key.properties
android/app/upload-keystore.jks

# Windows 本地 PowerShell 7 包装脚本（Flutter 3.24+ 需要 pwsh，本地用 PowerShell 5.1 替代）
pwsh.cmd

# 本地工具产物
.mimosa/
.playwright-cli/
.workbuddy/

# 桌面端生成产物
macos/Flutter/ephemeral/
windows/flutter/ephemeral/
windows/flutter/generated_plugins.cmake
windows/flutter/generated_plugin_registrant.cc
windows/flutter/generated_plugin_registrant.h

# drift/sqlite3 Web 运行资源与一次性开发脚本（本地磁盘保留，不入库）
/web/drift_worker.js
/web/sqlite3.wasm
/scripts/

# Flutter 本地工具状态
.flutter_tool_state
.hotrun.stdin
.hotrun.log
.hotrun.err.log
scripts/flutter_hot_daemon.ps1

# Local-only development material; keep the GitHub repository runnable and lean.
# 注：/test/ 已于 2026-09-10 移出忽略列表 —— 40+ 文件 / 7500+ 行测试是重要资产，
#     此前仅存在于本机、一旦丢失不可恢复，且 CI 依赖它们却拉不到。
# 注：/.github/ 与 analysis_options.yaml 已于 2026-09-10 入库 —— 否则 CI 工作流
#     与 lint 配置只存在于本机，"零告警"与发布流水线都无法从仓库复现
#     （决策：两项入库，并落地 A-3 日常提交门禁）。
/docs/
UI_REVIEW_FIX_GUIDE.txt
current_home*.png
menu.png
settings*.png
CHANGELOG.md
MCP_Implementation_Plan.md
Workspace_Coding_Agent_Plan.md
fix_chat_page.py
start.bat
