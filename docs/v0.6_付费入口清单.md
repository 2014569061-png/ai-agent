# v0.6 付费模块入口清单（仅登记，不改代码）

> 目标：本版聚焦使用体验。若 v0.6 不打算开放付费，发布前需确认这些入口不被用户点到空白页/报错。
> 状态：⬜ 待确认 ／ ✅ 已隐藏 ／ ⏸️ 本版不管

## 页面/入口

- [ ] `lib/presentation/account/account_page.dart` —— 账号页（登录/余额/权益/入口）
- [ ] `lib/presentation/account/billing_page.dart` —— 充值页
- [ ] `lib/presentation/account/usage_report_page.dart` —— 用量报表页
- [ ] `lib/presentation/account/compliance_page.dart` —— 合规页
- [ ] `lib/application/account_services.dart` —— 账号/权益服务
- [ ] `lib/application/billing_api.dart` —— 计费 API 客户端
- [ ] `lib/infrastructure/providers/proxy_provider.dart` —— 托管 Key / 内置额度 Provider

## 建议检查点

1. 导航入口（`app_shell.dart` / 设置页）是否仍展示账号/充值/内置额度入口
2. 若入口保留但后端未部署，点击后是否有统一提示「暂未开放」
3. 认证/权益逻辑是否会影响免费用户使用本地 Key（不能把本地 Key 用户锁死）