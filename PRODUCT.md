# NEXUS Agent 产品边界与验证目标

## 当前版本

- 验证版本：`v0.8.8`（Android `versionCode 90`）。
- 发布形态：Android-only，ARM64（`arm64-v8a`）APK，通过 GitHub Releases 侧载。
- 主要业务文案：中文；BYOK（用户自行配置模型服务商与 API Key）。

## 核心用户

愿意自行管理 API Key、通过侧载安装 Android 应用，并希望在移动设备上处理开发工作的开发者。

## 三个核心任务

1. 理解项目与代码审查：让 Agent 阅读工作区内容、解释结构并指出问题。
2. 执行受审批的开发任务：在用户确认风险和计划后调用工具完成开发工作。
3. 中断后恢复并审阅结果：任务退到后台或中断后继续执行，查看运行时间线、报告和结果。

## 北极星指标与护栏

北极星指标是“每周被用户标记为有帮助的开发任务数”。配套观察指标包括：

- 任务完成率；
- 中断后恢复成功率；
- 平均 Token 用量与成本；
- 审批拒绝率与误操作；
- 有帮助反馈的样本量。

## 验证目标

在发布前后的真实使用中，邀请 5–10 名开发者连续使用，并观察是否形成每周重复使用，以及上述北极星和护栏指标是否可稳定记录、解释和复核。

## 产品边界

### 非目标

本验证阶段不做以下事项：

- 新增模型 Provider；
- 商业化、计费或盈利化；
- 云同步、账户体系或服务端反馈平台；
- Web 或桌面发布；
- Play Store 合规改造；
- 知识库架构升级；
- 把终端包装成未经实现的完整安全沙箱。

### Android 侧载与权限

应用仅针对 Android ARM64 设备验证。用户从 GitHub Releases 下载 APK 侧载安装；系统可能要求允许本应用安装未知来源应用。网络、通知、前台服务、电池优化豁免等权限按功能需要请求，权限用途应在应用设置或系统提示中由用户确认。后台任务依赖 Android 调度与系统资源，定时触发是尽力而为，不承诺精确到点。

### 终端边界

工作区是默认工作目录，不等同于完整文件系统沙箱。解释器脚本可能访问工作区外文件或网络；终端属于需要确认的工具，在完全访问模式下可能自动执行。用户应在审批前检查工具参数、目标路径和命令影响范围。

## 验证重点

- 版本、包名、ARM64 ABI 和正式签名可从发布 APK 独立核验；
- 发布 tag 遵循 `v<versionName>`，本版本为 `v0.8.8`，build number 不进入 tag；
- APK 同时提供 SHA-256 sidecar，便于侧载前校验；
- 真实设备上人工确认安装未知来源、后台任务、恢复流程、审批边界和终端提示。
